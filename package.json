{
  "name": "corona-analytics",
  "version": "1.0.0",
  "description": "Lightweight Analytics",
  "main": "index.js",
  "scripts": {
    "dev": "tsup src/sdk.ts --format iife --watch --out-dir public",
    "build": "tsup src/sdk.ts --format iife --minify --clean --out-dir public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
